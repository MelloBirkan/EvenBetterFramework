# EvenBetter Android Skills

This plugin is an MVP placeholder. Android-specific EvenBetter skills have not been authored in this repository yet.

Keep this directory at the plugin root because Claude Code plugin components must live outside `.claude-plugin/`. Add future Android skill packages here as `skills/<skill-name>/SKILL.md`.
