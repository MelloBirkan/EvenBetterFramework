---
name: evenbetter-swiftui-accessibility
description: "Accessibility analysis rules for SwiftUI projects following Apple Human Interface Guidelines and WCAG"
---

# Accessibility Rules — SwiftUI (Apple HIG & WCAG)

Load the canonical accessibility clauses from `../../corpus/ios/accessibility.md`. Use `../../corpus/index.json` when clause metadata, source URLs, retrieval dates, anchors, or corpus version are needed.

Do not duplicate accessibility rule bodies in this skill. The corpus is the source of truth for stable clause IDs such as `A11Y-UI-001`, `A11Y-UX-001`, and `A11Y-A11Y-001`.

When asked for SwiftUI accessibility guidance, answer from the corpus clauses and preserve their `Severity`, `Dimension`, `Source`, `Retrieved`, `Check`, `Why`, and `Correct code` fields.
